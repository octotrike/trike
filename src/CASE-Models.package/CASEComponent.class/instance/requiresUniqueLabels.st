accessing
requiresUniqueLabels
	"Answer whether each of my components is required to have a unique label.  True by default, but subclasses must set to false if each type of component has its own name space."
	
	^requiresUniqueLabels
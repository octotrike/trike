instance creation
initializedInstance
	"Answer a pre-populated, largely read-only entity model describing the entities used by an EMEntityModel.  The rules portion of this meta-entity model is used to select and enforce the rules for a corresponding instance of EMEntityModel."
	
	^self designated: 'Meta'